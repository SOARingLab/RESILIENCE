import { NonFunctionalVerificationResult } from './non-functional-verification-result';

describe('NonFunctionalVerificationResult', () => {
  it('should create an instance', () => {
    expect(new NonFunctionalVerificationResult()).toBeTruthy();
  });
});
