import { FunctionalVerificationResult } from './functional-verification-result';

describe('FunctionalVerificationResult', () => {
  it('should create an instance', () => {
    expect(new FunctionalVerificationResult()).toBeTruthy();
  });
});
