import { GetProcessInstance } from './get-process-instance';

describe('GetProcessInstance', () => {
  it('should create an instance', () => {
    expect(new GetProcessInstance()).toBeTruthy();
  });
});
